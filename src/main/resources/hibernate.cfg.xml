<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- JDBC Database connection settings -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/search_engine?useSSL=false</property>
        <property name="connection.username">sa</property>
<!--        <property name="connection.password"></property>-->
        <!-- JDBC connection pool settings ... using built-in test pool -->
        <property name="hibernate.current_session_context_class">thread</property>
        <property name="connection.pool_size">10</property>
        <!-- Select our SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQL5Dialect</property>
        <!-- Echo the SQL to stdout -->
        <property name="show_sql">true</property>
        <!-- Set the current session context -->
        <property name="current_session_context_class">thread</property>
        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

        <mapping class="main.model.Page"></mapping>
        <mapping class="main.model.Index"></mapping>
        <mapping class="main.model.Lemma"></mapping>
        <mapping class="main.model.Field"></mapping>

    </session-factory>
</hibernate-configuration>